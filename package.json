{
  "name": "cfdev-plugin-e2e",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "cy:open": "cypress open",
    "cy:run": "cypress run --browser chrome --spec 'cypress/e2e/*.cy.js'",
    "cy:run:headed": "cypress run --browser chrome --headed"
  },
  "devDependencies": {
    "cypress": "^13.14.2"
  }
}

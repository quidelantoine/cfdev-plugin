## CFDev Public API

### register_cfdev_post_type()
Enregistre un custom post type.

**Paramètres**
- `$name` (string|array) — Nom singulier, ou [singulier, pluriel]
- `$args` (array) — Arguments de register_post_type()
- `$labels` (array) — Labels personnalisés

**Exemple**
```php
// A ajouter 
```